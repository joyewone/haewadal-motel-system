# 실제 판매용 모텔 객실관리시스템 전환 검토 (현행 코드 기준)

## 문서 목적
이 문서는 현재 저장소의 **Python 데스크톱 버전(`windows_app`)**과 **Windows 네이티브 버전(`windows_native_app`)**을 기준으로,
실제 판매(유상 도입/유지보수 계약/다지점 운영) 가능한 제품으로 전환하기 위한 수정 사항을 우선순위별로 정리합니다.

---

## 1) 현재 상태 요약 (As-Is)

### 이미 구현된 것
- 객실 시드(30개), 객실 상태 전환(이용가능/사용중/청소중), 체크인/체크아웃, 일 매출 합계가 구현되어 있습니다.
- SQLite 로컬 DB를 사용해 단일 PC 환경에서는 즉시 동작 가능합니다.
- Python 버전은 핵심 저장소 로직에 대한 단위 테스트가 있습니다.

### 판매용 전환 관점의 한계
- **단일 단말 로컬 DB 구조**여서 다중 사용자 동시 사용, 본사-지점 통합 운영, 중앙 백업/복구가 어렵습니다.
- **인증/권한/감사로그**가 없어 직원 오조작·부정행위·사후 추적 대응이 불가능합니다.
- **결제/환불/정산/세무 증빙** 등 실제 운영 필수 도메인 기능이 부족합니다.
- **배포·업데이트·장애 모니터링 체계**가 없어 상용 유지보수 SLA 대응이 어렵습니다.

---

## 2) 코드 기반 핵심 리스크 (증거)

### A. 권한/보안/감사 부재 (P0)
- Python/Native 모두 로그인·권한 검증 없이 버튼 동작으로 객실 상태가 변경됩니다.
- 누가 언제 어떤 객실 정보를 바꿨는지 추적 가능한 감사 로그가 없습니다.

### B. 동시성/무결성 제어 미흡 (P0)
- Native `CheckIn`은 현재 객실 상태 검사 없이 바로 `occupied`로 업데이트합니다.
- Native `UpdateStatus`도 상태 전이 규칙(예: 사용중 객실을 임의 이용가능 처리 금지)을 강제하지 않습니다.
- Python은 일부 검증이 있으나 트랜잭션/락 전략이 없어 다중 프로세스 환경에서 경합 위험이 있습니다.

### C. 데이터 모델 상용성 부족 (P1)
- `stay_history`는 단순 이력만 저장하며 결제 수단/환불/할인/부가세/영수증 번호 등 회계 필드가 없습니다.
- 지점/사업자/사용자(직원) 식별자가 없어 멀티지점 운영과 접근 통제가 불가능합니다.

### D. 운영 체계 미구축 (P1)
- 자동 백업, 복구 리허설, 장애 알림, 원격 로그 수집 체계가 없습니다.
- 설치 후 자동 업데이트 경로(버전 채널, 롤백)가 없어 현장 배포 비용이 큽니다.

### E. 테스트 범위 편중 (P2)
- Python 저장소 테스트는 있으나 UI/통합 테스트가 없습니다.
- Native 앱은 자동 테스트가 없어 회귀 검증이 수동에 의존합니다.

---

## 3) 우선순위별 수정 계획

## P0 (즉시: 1~2주) — “사고 방지”

1. **권한 체계 최소 도입**
   - 직원 로그인(ID/PW 또는 Windows 계정 연동)
   - 역할 분리: 관리자/카운터/청소
   - 모든 상태변경·요금수정 API에 권한 체크

2. **상태 전이 규칙 강제**
   - 허용 전이 테이블 도입 (예: available→occupied, occupied→cleaning 등)
   - 체크인 시 점유 객실/미존재 객실/음수요금 방지
   - 체크아웃/상태변경을 트랜잭션으로 묶어 원자성 보장

3. **감사 로그 필수화**
   - `audit_logs(id, actor_id, action, room_number, before_json, after_json, created_at)` 추가
   - UI 액션(체크인/체크아웃/상태변경/요금수정) 100% 로그 적재

4. **DB 안전장치 추가**
   - CHECK 제약(status enum, price >= 0)
   - 핵심 인덱스(상태, 체크아웃일, 업데이트시각)

## P1 (단기: 3~6주) — “판매 가능한 기능”

1. **정산/회계 기능 확장**
   - 결제수단(현금/카드/이체), 할인, 환불, 미수금
   - 근무교대 마감(Shift Close), 일마감/월마감 리포트
   - 영수증/거래내역 출력 및 재출력 기록

2. **멀티지점 데이터 모델 도입**
   - `tenant_id`, `branch_id`, `user_id` 기반 스키마 분리
   - 지점별 데이터 접근 정책 및 조회 범위 제한

3. **운영성 강화**
   - 백업 스케줄 + 복구 테스트 절차 문서화
   - 장애 로그 수집(Sentry/OpenTelemetry 등) 및 알림 연동
   - 설치본 버전관리와 자동 업데이트(채널: stable/beta)

## P2 (중기: 7~12주) — “확장/표준화”

1. **아키텍처 고도화**
   - 단말 로컬 DB 중심에서 서버-클라이언트 구조로 전환
   - API 레이어(인증/권한/검증) + 중앙 DB + 캐시/큐 도입 검토

2. **품질 체계 정착**
   - Native/Python 공통 도메인 테스트 스위트
   - CI 파이프라인(빌드/테스트/패키징/아티팩트 서명)
   - 릴리즈 노트 자동화 + 회귀 테스트 체크리스트

3. **규정 대응**
   - 개인정보 최소수집·보관기간·마스킹·파기 정책
   - 로그 보존 정책(예: 1년/3년) 및 접근통제 기록

---

## 4) 권장 제품 형태 (의사결정 가이드)

### 옵션 A: 단일 지점 오프라인 특화 제품
- 장점: 빠른 출시, 낮은 인프라 비용
- 한계: 본사 통합관리/원격지원/실시간 집계 제약
- 적합: 1개 지점 독립 운영, 저가형 상품

### 옵션 B: 중앙 서버형 SaaS/온프렘 하이브리드
- 장점: 멀티지점/권한/감사/백업/리포트 확장 용이
- 한계: 초기 개발비·운영복잡도 증가
- 적합: 실제 판매용 주력 상품(권장)

---

## 5) 30일 실행 로드맵 (현실형)

### 1주차
- 상태 전이 규칙/입력 검증/트랜잭션 공통 모듈화
- 감사 로그 테이블 및 로깅 훅 추가

### 2주차
- 로그인/권한(관리자·카운터·청소) 적용
- 객실 이벤트(체크인/체크아웃/상태변경) 권한 정책 반영

### 3주차
- 결제수단/환불/할인/근무교대 마감 데이터 모델 확장
- 일마감 리포트(매출, 객실가동률, 취소/환불) 구현

### 4주차
- 설치/업데이트/백업·복구 운영 매뉴얼 완성
- 파일럿 지점 PoC 후 장애·UX 개선 반영

---

## 6) 최종 결론
현재 구현은 **"현장 데모 및 단일 PC 운영"** 수준으로는 의미가 있지만,
실제 판매용 제품으로 보려면 최소한 **권한·감사·무결성·정산·운영 체계**를 먼저 보강해야 합니다.

권장 순서는 다음과 같습니다.
1) **P0 안전장치(사고 방지)** → 2) **P1 수익 기능(정산/리포트/지점화)** → 3) **P2 확장/표준화**.
